# Git LFS 配置文件

# 配置大型数据文件使用Git LFS管理
*.dat filter=lfs diff=lfs merge=lfs -text
*.bin filter=lfs diff=lfs merge=lfs -text
*.parm filter=lfs diff=lfs merge=lfs -text

# 确保二进制文件不会被Git自动转换行尾
*.dll binary
*.a binary
*.o binary
*.obj binary
*.so binary
*.dylib binary

# 确保Fortran和Python文件的行尾处理正确
*.f text eol=lf
*.f90 text eol=lf
*.F90 text eol=lf
*.py text eol=lf

# 配置文本文件的行尾处理
*.txt text eol=lf
*.md text eol=lf
*.ps1 text eol=crlf  # PowerShell脚本使用CRLF
